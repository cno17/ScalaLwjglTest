package yage.shape.util

import org.joml.Vector2f
import org.joml.Vector3f

class IntersectionResult3:
  
  var num = 0
  
  val p1 = Vector3f()
  val p2 = Vector3f()

  // inside, outside info

  // used by Intersectionf  
  
  val vec2 = Vector2f()
  val vec3 = Vector3f()
